# For Firebase Emulator local testing only.
# Copy to .secret.local and replace the placeholder when running functions locally.
# Never commit the real key.

GEMINI_API_KEY=replace-with-your-own-gemini-key
